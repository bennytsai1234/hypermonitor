# 線性比例跟單策略邏輯 (Incremental Ratio Strategy)

## 📌 策略核心
將 Hyperliquid 大戶的淨壓差變動 (Delta)，等比例轉化為 OKX 實盤交易金額。

## 🧮 數學模型
- **變量定義**:
  - `D_H`: Hyperliquid 全體淨壓差變化量 (Current Net - Previous Net)
  - `R`: 縮放比例 = `1/10,000` (每 100 萬美元變動對應 100 美元操作)
  - `Order_USD`: 最終下單名義價值 = `D_H * R`

## 🛠️ 執行細節 (OKX V5)
1. **單位換算**: 
   - OKX 下單需要 `sz` (張數或幣數)。
   - 公式: `sz = Order_USD / (Current_Price * Contract_Multiplier)`
2. **交易方向**:
   - `D_H > 0`: `side: buy`, `posSide: long` (增加多頭曝光)
   - `D_H < 0`: `side: sell`, `posSide: short` (增加空頭曝光)
3. **過濾器 (Noise Filters)**:
   - **最小閾值 (Minimum Threshold)**: 建議 `|D_H|` 需大於 50 萬美元才觸發，避免頻繁交易導致的手續費磨損與小幅雜訊。
   - **最大上限 (Max Exposure)**: 設定單次下單上限（例如不超過 2,000 USD）與總倉位上限，防止數據異常導致爆倉。

## 📋 開發計畫
1. [ ] **資料庫查詢**: 修改 Worker 邏輯，每次 `update-printer` 時取出上一次的 `net_vol_num` 計算差值。
2. [ ] **價格獲取**: 調用 `GET /api/v5/market/ticker` 獲取即時市價以換算 `sz`。
3. [ ] **模擬執行**: 在 OKX Demo 帳戶進行線性映射測試。
